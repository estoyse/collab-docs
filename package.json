{
  "name": "collab-docs",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"pnpm -F @collab-docs/server dev\" \"pnpm -F @collab-docs/web dev\"",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "10.0.5",
    "typescript": "^5.7.0"
  }
}
